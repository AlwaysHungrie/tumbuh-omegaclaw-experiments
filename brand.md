# Brand — Tumbuh

_Status: deferred_

The user chose to defer the interactive `brand-design` flow. The Tumbuh presentation site uses a hand-picked organic palette applied directly in the Next.js app (`site/`): deep botanical green as primary, warm earth/amber as accent, soft cream/bone backgrounds in light mode and deep forest-charcoal in dark mode. Typography pairs a geometric sans for UI with a mono for code/scores.

To set up a full brand palette, typography, and voice interactively at any time, run:

    /brand-design

or say: "pick brand colors"

When `brand-design` runs, it will detect this deferred state, skip the "confirm overwrite" step, and proceed directly to full brand setup, then replace this file.
